---
description: Start the development server for the portfolio
---

1. Ensure dependencies are installed by running `npm install` gracefully.
2. Run the development environment with `npm run dev`.
3. Open the browser at `http://localhost:5173`.
4. Validate that no runtime React or context errors appear.
